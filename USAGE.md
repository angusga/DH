# 前端使用说明（无需后端）

## 功能说明

当前版本直接从 `public/dld.geojson` 文件加载管线数据，无需连接数据库和启动后端服务。

## 使用方法

### 1. 启动前端服务

```bash
cd frontend
npm install
npm run dev
```

### 2. 使用导航功能

1. **查看管线数据**
   - 地图会自动加载 `public/dld.geojson` 文件
   - 每条管线会显示为蓝色折线
   - 每条管线的起点显示为蓝色圆点，终点显示为橙色圆点

2. **选择起点**
   - 点击地图上任意管线的起点标记（蓝色圆点）
   - 起点会显示为绿色高亮标记

3. **选择终点**
   - 点击地图上任意管线的终点标记（橙色圆点）
   - 终点会显示为红色高亮标记

4. **查看导航路径**
   - 当起点和终点都选择后，会自动计算并显示路径
   - 路径显示为橙色粗线
   - 控制面板会显示距离和路径点数

5. **手动开始导航**
   - 也可以点击"导航"按钮打开控制面板
   - 在控制面板中点击"开始导航"按钮

## 数据格式

`dld.geojson` 文件包含以下字段：
- `ID`: 点ID
- `PolyLineID`: 管线ID
- `PLPointID`: 点在管线中的序号
- `PLPointPos`: 经度
- `PLPointP_1`: 纬度

## 导航算法

当前版本使用简化算法：
- **同一条管线**：直接连接起点和终点之间的所有点
- **不同管线**：直线连接起点和终点（后续可优化为跨管线路径规划）

## 注意事项

1. 确保 `public/dld.geojson` 文件存在
2. 高德地图API Key和安全密钥需要正确配置
3. 如果地图不显示，检查浏览器控制台的错误信息

